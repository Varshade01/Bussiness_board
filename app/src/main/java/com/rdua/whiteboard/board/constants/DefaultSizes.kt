package com.rdua.whiteboard.board.constants

import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

object DefaultSizes {
    val stickySize = DpSize(164.dp, 164.dp)
    val shapeSize = DpSize(100.dp, 100.dp)
    val textSize = DpSize(48.dp, 48.dp)
    val frameSize = DpSize(256.dp, 256.dp)
    val lineSize = DpSize(width = 100.dp, height = 2.dp)

    val minHandleInteractiveSize = 25.dp
}

